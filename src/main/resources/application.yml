spring:
  application:
    name: troubleshootingApp
  datasource:
    url: jdbc:h2:mem:testd
    driver-class-name: org.h2.Driver
    username: test
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
